Example:
```cs
public struct RgbColor{
	public int Red;
	public int Green;
	public int Blue;
}
```

Use a structure when you want a small, lightweight object, or you're dealing with something that has a point, like (x, y).

Don't really need this most of the time though.